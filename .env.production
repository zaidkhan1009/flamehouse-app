# ==============================================================================
# FLAMEHOUSE FRONTEND (FLUTTER) - PRODUCTION ENVIRONMENT CONFIGURATION
# ==============================================================================
# Copy this to .env at build time for the production environment.

# Production backend API URL (e.g., Render live deploy or custom domain)
API_BASE_URL=https://viral-bytes.onrender.com/api/v1

# Production settings
AI_FEATURE_ENABLED=true
DEFAULT_TENANT_ID=default
